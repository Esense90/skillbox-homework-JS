let name = 'кОНстанТин';

let fearstLetterName = name.substr(0, 1);

let nextLetterName = name.substr(1);

if (fearstLetterName == fearstLetterName.toUpperCase() && nextLetterName == nextLetterName.toLowerCase()) {

} else(fearstLetterName == fearstLetterName.toLowerCase()); {
  let regLetterName = fearstLetterName.toUpperCase();
  let regNextLettername = nextLetterName.toLowerCase();
  console.log(regLetterName + regNextLettername);
}
